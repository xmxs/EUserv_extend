name: 'EUserv Auto Extend'

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '4 2  * * 1,4'

jobs:
  auto_renew:
    runs-on: ubuntu-18.04
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v2

      - name: 'Set python'
        uses: actions/setup-python@v1
        with:
          python-version: '3.x'

      - name: 'maintain the mfkj'
        run: curl 'http://hewp.bdtc.cf/' --resolv hewp.bdtc.cf:80:80.78.132.207 -H 'Proxy-Connection: keep-alive'   -H 'Cache-Control: max-age=0'   -H 'DNT: 1'   -H 'Upgrade-Insecure-Requests: 1'   -H 'User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/78.0.3904.108 Safari/537.36'   -H 'Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9'   -H 'Accept-Language: zh-CN,zh;q=0.9'   -H 'Cookie: __cfduid=d66b9a5fdbb0f232f0287eb96b719fd9e1613981366'   --compressed   --insecure

      - name: 'Install dependencies'
        run: |
          python -m pip install --upgrade requests
          python -m pip install --upgrade beautifulsoup4

      - name: 'Auto_renew'
        env:
          USERNAME: ${{ secrets.USERNAME }}
          PASSWORD: ${{ secrets.PASSWORD }}
        run: python main.py
